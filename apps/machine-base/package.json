{
  "name": "@emp/machine-base",
  "version": "1.0.0",
  "description": "Base machine foundation for EmProps infrastructure",
  "main": "src/index.js",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "node src/index.js",
    "dev": "NODE_ENV=development node src/index.js",
    "health-check": "curl -f http://localhost:9090/health",
    "test": "vitest",
    "test:watch": "vitest --watch",
    "test:coverage": "vitest --coverage"
  },
  "dependencies": {
    "ioredis": "^5.3.2",
    "winston": "^3.11.0",
    "express": "^4.18.2",
    "dotenv": "^16.3.1",
    "uuid": "^9.0.1",
    "execa": "^8.0.1"
  },
  "devDependencies": {
    "vitest": "^1.0.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}