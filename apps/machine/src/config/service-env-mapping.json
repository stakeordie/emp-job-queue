{
  "comfyui": {
    "COMFYUI_HOST": "${WORKER_COMFYUI_HOST:-localhost}",
    "COMFYUI_PORT": "${WORKER_COMFYUI_PORT:-8188}",
    "COMFYUI_TIMEOUT_SECONDS": "${WORKER_COMFYUI_TIMEOUT_SECONDS:-300}",
    "COMFYUI_MAX_CONCURRENT_JOBS": "${WORKER_COMFYUI_MAX_CONCURRENT_JOBS:-1}",
    "COMFYUI_USERNAME": "${WORKER_COMFYUI_USERNAME:-}",
    "COMFYUI_PASSWORD": "${WORKER_COMFYUI_PASSWORD:-}"
  },
  "simulation": {
    "SIMULATION_MAX_CONCURRENT_JOBS": "${WORKER_SIMULATION_MAX_CONCURRENT_JOBS:-1}"
  },
  "text_generation": {
    "OPENAI_API_KEY": "${OPENAI_API_KEY:-}",
    "OPENAI_BASE_URL": "${OPENAI_BASE_URL:-https://api.openai.com/v1}",
    "OPENAI_TEXT_MODEL": "${OPENAI_TEXT_MODEL:-gpt-4}",
    "OPENAI_MAX_TOKENS": "${OPENAI_MAX_TOKENS:-4000}",
    "OPENAI_TEMPERATURE": "${OPENAI_TEMPERATURE:-0.7}",
    "OPENAI_TIMEOUT_SECONDS": "${OPENAI_TIMEOUT_SECONDS:-60}",
    "OPENAI_RETRY_ATTEMPTS": "${OPENAI_RETRY_ATTEMPTS:-3}",
    "OPENAI_TEXT_MAX_CONCURRENT_JOBS": "${OPENAI_TEXT_MAX_CONCURRENT_JOBS:-5}"
  },
  "image_generation": {
    "OPENAI_API_KEY": "${OPENAI_API_KEY:-}",
    "OPENAI_BASE_URL": "${OPENAI_BASE_URL:-https://api.openai.com/v1}",
    "OPENAI_IMAGE_MODEL": "${OPENAI_IMAGE_MODEL:-dall-e-3}",
    "OPENAI_IMAGE_SIZE": "${OPENAI_IMAGE_SIZE:-1024x1024}",
    "OPENAI_IMAGE_QUALITY": "${OPENAI_IMAGE_QUALITY:-standard}",
    "OPENAI_TIMEOUT_SECONDS": "${OPENAI_TIMEOUT_SECONDS:-120}",
    "OPENAI_RETRY_ATTEMPTS": "${OPENAI_RETRY_ATTEMPTS:-3}",
    "OPENAI_IMAGE_MAX_CONCURRENT_JOBS": "${OPENAI_IMAGE_MAX_CONCURRENT_JOBS:-3}"
  },
  "ollama": {
    "OLLAMA_HOST": "${OLLAMA_HOST:-localhost}",
    "OLLAMA_PORT": "${OLLAMA_PORT:-11434}",
    "OLLAMA_DEFAULT_MODEL": "${OLLAMA_DEFAULT_MODEL:-llama3}",
    "OLLAMA_MAX_CONCURRENT_JOBS": "${OLLAMA_MAX_CONCURRENT_JOBS:-1}"
  },
  "a1111": {
    "A1111_HOST": "${A1111_HOST:-localhost}",
    "A1111_PORT": "${A1111_PORT:-7860}",
    "A1111_TIMEOUT_SECONDS": "${A1111_TIMEOUT_SECONDS:-300}",
    "A1111_MAX_CONCURRENT_JOBS": "${A1111_MAX_CONCURRENT_JOBS:-1}"
  },
  "replicate": {
    "REPLICATE_API_TOKEN": "${REPLICATE_API_TOKEN:-}",
    "REPLICATE_MAX_CONCURRENT_JOBS": "${REPLICATE_MAX_CONCURRENT_JOBS:-3}"
  }
}